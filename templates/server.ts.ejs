// Fix preact module aliasing issue
import 'module-alias/register'

// Load preact kit
import { createApp } from "preact-kit";

async function main() {
  const app = await createApp();

  app.listen(process.env.PORT || 3000, () =>
    console.log("🚀 Serving <%= appName %> on http://localhost:3000")
  );
}

main();